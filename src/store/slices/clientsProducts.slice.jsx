import { createSlice } from '@reduxjs/toolkit';

export const clientsProductsSlice = createSlice({
    name: 'clientsProducts',
    initialState: [],
    reducers: {

    }
})

export const {  } = clientsProductsSlice.actions;

export default clientsProductsSlice.reducer;
